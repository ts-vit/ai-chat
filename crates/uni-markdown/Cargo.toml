[package]
name = "uni-markdown"
version = "0.1.0"
edition = "2021"
description = "Markdown parsing and transformation for UNI Framework"
license = "MIT"

[dependencies]
uni-common = { path = "../uni-common" }
pulldown-cmark = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
