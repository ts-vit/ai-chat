[package]
name = "uni-settings"
version = "0.1.0"
edition = "2021"

[dependencies]
uni-common = { path = "../uni-common" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"

[dev-dependencies]
tempfile = "3"
