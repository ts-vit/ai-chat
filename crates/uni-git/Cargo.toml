[package]
name = "uni-git"
version = "0.1.0"
edition = "2021"
description = "Git operations for UNI Framework (powered by gitoxide)"
license = "MIT"

[dependencies]
uni-common = { path = "../uni-common" }
gix = { version = "0.81" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
